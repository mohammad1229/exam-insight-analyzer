# نظام تحليل نتائج الاختبارات المدرسية

## 🚀 التشغيل السريع

### التثبيت
```bash
npm install
```

### طرق التشغيل

#### 1. تشغيل الويب (أونلاين)
```bash
npm run build && npm run preview
```
ثم افتح المتصفح على: http://localhost:4173

#### 2. تشغيل التطوير
```bash
npm run dev
```
ثم افتح المتصفح على: http://localhost:8080

#### 3. تشغيل سطح المكتب (Electron)

##### التشغيل المباشر (تلقائي)
```bash
# وضع الإنتاج - يبني ويشغل تلقائياً
node scripts/electron-start.js

# وضع التطوير
node scripts/electron-start.js --dev
```

##### بناء تطبيق سطح المكتب للتوزيع
```bash
# Windows
node scripts/build-desktop.js win

# macOS
node scripts/build-desktop.js mac

# Linux
node scripts/build-desktop.js linux
```

#### 4. التشغيل التلقائي الشامل
```bash
# تشغيل الويب (افتراضي) - يثبت ويبني ويشغل
node scripts/start-app.js

# تشغيل Electron
node scripts/start-app.js --electron

# تشغيل المعاينة
node scripts/start-app.js --preview

# تشغيل التطوير
node scripts/start-app.js --dev
```

---

## 📱 ميزات النظام

### 🌐 العمل أونلاين وأوفلاين (PWA)
- النظام يعمل كتطبيق ويب تقدمي (PWA)
- يمكن تثبيته على الهاتف أو الكمبيوتر من المتصفح
- البيانات تُحفظ محلياً وتُزامن تلقائياً عند الاتصال بالإنترنت
- العمليات المعلقة تُنفذ عند عودة الاتصال

### 🖥️ تطبيق سطح المكتب (Electron)
- يعمل مع أو بدون اتصال بالإنترنت
- قاعدة بيانات SQLite محلية
- مزامنة تلقائية مع السحابة عند الاتصال
- اختصار على سطح المكتب

### 📊 إدارة الاختبارات
- إنشاء وتعديل وحذف الاختبارات
- إدخال وتعديل نتائج الطلاب
- تحليل شامل للنتائج والأسئلة
- تقارير PDF مفصلة

---

## 📁 هيكل المشروع

```
├── dist/                 # ملفات البناء
├── release/             # تطبيقات سطح المكتب المجمعة
├── resources/           # موارد Electron (أيقونات)
│   └── icon.svg
├── scripts/             # سكريبتات التشغيل التلقائي
│   ├── start-app.js     # التشغيل الشامل
│   ├── electron-start.js # تشغيل Electron
│   └── build-desktop.js  # بناء التطبيق
├── src/
│   ├── electronMain.js  # العملية الرئيسية لـ Electron
│   ├── preload.js       # سكريبت التحميل المسبق
│   ├── services/
│   │   ├── electronService.ts   # خدمة Electron
│   │   └── offlineSyncService.ts # خدمة المزامنة
│   └── components/
│       └── OfflineIndicator.tsx  # مؤشر الاتصال
├── electron-builder.json # إعدادات بناء Electron
└── vite.config.ts        # إعدادات Vite + PWA
```

---

## 🛠️ المتطلبات

- **Node.js** 18 أو أحدث
- **npm** أو **yarn**
- لـ Electron: نظام Windows/macOS/Linux

---

## 🔒 الترخيص
جميع الحقوق محفوظة

## الميزات الإضافية

### 1. نسخ احتياطي واستعادة البيانات
يمكن إضافة وظائف لنسخ قاعدة البيانات واستعادتها:

```javascript
// في main.js
ipcMain.handle('backup-database', async () => {
  const backupPath = path.join(appPath, `backup-${Date.now()}.db`);
  return new Promise((resolve, reject) => {
    const backup = fs.createWriteStream(backupPath);
    const source = fs.createReadStream(path.join(appPath, 'database.db'));
    
    source.pipe(backup);
    source.on('end', () => resolve(backupPath));
    source.on('error', (err) => reject(err));
  });
});

ipcMain.handle('restore-database', async (event, backupPath) => {
  const dbPath = path.join(appPath, 'database.db');
  return new Promise((resolve, reject) => {
    db.close((err) => {
      if (err) {
        reject(err);
        return;
      }
      
      const backup = fs.createReadStream(backupPath);
      const current = fs.createWriteStream(dbPath);
      
      backup.pipe(current);
      current.on('finish', () => {
        // إعادة فتح قاعدة البيانات
        db = new sqlite3.Database(dbPath);
        resolve(true);
      });
      current.on('error', (err) => reject(err));
    });
  });
});
```

### 2. تحديثات تلقائية
يمكن إضافة ميزة التحديث التلقائي باستخدام electron-updater.

## الخلاصة
باتباع هذه الخطوات، يمكن تحويل تطبيق الويب إلى تطبيق سطح مكتب كامل مع القدرة على تخزين البيانات محلياً في قاعدة بيانات SQLite، والقدرة على إنشاء اختصار على سطح المكتب، وتثبيت التطبيق مباشرة على جهاز المستخدم.
